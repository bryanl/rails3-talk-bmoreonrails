<% if flash[:notice] %>
  <div class="notice">
    <%= flash[:notice] %>
  </div>
<% end %>

<ul>
  <% @posts.each do |post|%>
    <li><%= link_to post.title, [post] %></li>
  <% end %>
</ul>

<%= link_to "Create a new post", new_post_path %>
